Backend para chat en tiempo real (Express + TypeScript + Socket.IO + MongoDB).

Run locally:

1. Copia `.env.example` a `.env` y ajusta `MONGO_URI` y `JWT_SECRET`.
2. Instala dependencias con pnpm: `pnpm install`.
3. Inicia en modo dev: `pnpm dev` (usa ts-node-dev).
